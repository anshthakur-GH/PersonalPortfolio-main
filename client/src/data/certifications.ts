export interface Certification {
  title: string;
  issuer: string;
  date: string;
  description: string;
  link: string;
  icon: string;
  isAchievement: boolean;
}

export const certificationsData: Certification[] = [
  // Certifications will be added later
];

export const achievementsData: Certification[] = [
  // Achievements will be added later
];
